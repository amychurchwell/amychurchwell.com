<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="shortcut icon" type="image/png" href="img/favicon.ico" />
        <link rel="stylesheet" href="css/style.css" />
        <title>{{ title }}</title>
    </head>

    <body>
        <header>Hello, welcome to my website.</header>
        <main class="container">
            <p>
                My name is Amy. I’m a {{ resume.title }}. I’ve been working at
                {{ resume.experience[0].company }} since
                {{ resume.experience[0].startDate }}. {% year %}
            </p>

            <!-- {% if github.commit %}
            <section class="github-commit">
                <p>latest site commit:</p>
                <date>{{ github.commit.date }}</date>
                <p>“{{ github.commit.msg }}” - {{ github.commit.author }}</p>
            </section>
            {% endif %} -->
        </main>

        <section class="mars-rover">
            <table>
                <thead>
                    <tr>
                        <th colspan="2">Recent Mars Photo</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td>
                            <img src="{{ mars.img }}" />
                        </td>
                    </tr>
                    <tr>
                        <td>rover:</td>
                        <td>{{ mars.rover}}</td>
                    </tr>
                    <tr>
                        <td>date:</td>
                        <td>{{ mars.date }}</td>
                    </tr>
                </tbody>
            </table>
        </section>
    </body>
</html>
